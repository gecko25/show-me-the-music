import type { NextPage } from "next";
import Image from "next/image";
import styles from "./Footer.module.scss";
import { Event } from "../../types";

type Props = {};

const DatePicker: NextPage = ({}: Props) => <div>DatePicker</div>;

export default DatePicker;
